/** @type {import('next').NextConfig} */
const nextConfig = {
  // Explicitly tell Vercel this is a Next.js app
  // No custom entrypoint needed - App Router uses app/ directory automatically
};

module.exports = nextConfig;

